int v()
{
	char str[520];
	int off = 0;

	fgets(str, 512, stdin);
	printf(str);
	if (off == 64) {
			fwrite("Wait what?!\n", 1, 12, stdout);
			system("/bin/sh");
	}
	return (0);
}

int main()
{
	v();
	return (0);
}